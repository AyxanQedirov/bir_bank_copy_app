import 'package:flutter/material.dart';

class ShadowStatics {
  BoxShadow cardBowShadow = BoxShadow(
      color: Colors.black.withOpacity(0.115),
      spreadRadius: 0.2,
      blurRadius: 3.2,
      offset: const Offset(0, 1.10));
}
